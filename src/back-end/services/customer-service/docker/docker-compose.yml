version: '3.4'

services:
  customer-service:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "5000:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
