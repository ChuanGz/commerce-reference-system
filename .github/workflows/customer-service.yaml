name: CI/CD for Customer Service

on:
  push:
    paths:
      - 'src/services/customer-service/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    # Step 1: Checkout the repository
    - name: Checkout repository
      uses: actions/checkout@v3

    # Step 2: Setup .NET
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0'

    # Step 3: Restore dependencies
    - name: Restore dependencies
      run: dotnet restore src/services/customer-service

    # Step 4: Build the service
    - name: Build
      run: dotnet build src/services/customer-service --no-restore

    # Step 5: Run tests
    - name: Run tests
      run: dotnet test src/services/customer-service --no-build

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
    # Deployment placeholder step
    - name: Deploy
      run: echo "Deploying Customer Service..."
